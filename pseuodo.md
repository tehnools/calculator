Initiailize variables and constants like total or input string


// Handlers

inputChangeHandler(event){
    inputString = event text content
}

inputNumberHandler(event){
    inputString += event.target.value
}

calculate(inputString){
    return calculation
}